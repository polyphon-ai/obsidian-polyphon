{
  "name": "obsidian-polyphon",
  "version": "0.1.0",
  "description": "Obsidian plugin that connects to a running Polyphon instance for multi-voice AI conversations",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "test": "vitest run --config vitest.unit.config.ts && vitest run --config vitest.unit.dom.config.ts && vitest run --config vitest.integration.config.ts",
    "test-unit": "vitest run --config vitest.unit.config.ts && vitest run --config vitest.unit.dom.config.ts",
    "test-integration": "vitest run --config vitest.integration.config.ts",
    "test-e2e": "vitest run --config vitest.e2e.config.ts",
    "lint": "eslint src"
  },
  "keywords": [
    "obsidian",
    "ai",
    "plugin",
    "polyphon",
    "multi-voice"
  ],
  "author": "Corey Daley",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.4.0",
    "@typescript-eslint/eslint-plugin": "^8.57.0",
    "@typescript-eslint/parser": "^8.57.0",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.27.3",
    "eslint": "^10.0.3",
    "eslint-plugin-obsidianmd": "^0.1.9",
    "jsdom": "^28.1.0",
    "obsidian": "latest",
    "playwright": "^1.58.2",
    "tslib": "^2.4.0",
    "typescript": "^5.0.4",
    "vitest": "^4.0.18"
  }
}
